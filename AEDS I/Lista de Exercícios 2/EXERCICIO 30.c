#include <stdio.h>
#include <stdlib.h>
#include <math.h>
#include <string.h>
/*
int main(){
    int i,aux=1;
    float soma=0;
    while(aux){
        printf("Digite um numero ou 0 para terminar: \n");
        scanf("%d",&aux);
        printf("Voce digitou %d \n",aux);
        if(aux){
            for(i=1;i<=aux;i++){
                soma+=i;
                printf("O resultado da serie e: %d", soma);
            }
            else{
                (printf("entrada terminada");
            }
        }
    }
return 0;
}

    int i,sinal=1;
    float soma=0;
    int aux=1 ;
    while(aux){
        printf("Digite um número qualquer ou 0 para terminar");
        scanf("%d", &aux);
        printf("Usuário digitou %d \n",aux);
        if(aux){
          printf("Vamos somar até %d \n",2*aux-1);
          for (i=1;i<=(2*aux-1);i++){
              soma+=sinal*i;
              sinal=-sinal;
            };
            printf("O resultado da série é %f \n\n",soma);
        }
        else{
             printf("entrada terminada \n");
        }
    }
    return 0;
}

    int i;
    float soma;
    int aux=1;
    soma =0;
    while(aux){
        printf("Digite um número qualquer ou 0 para terminar");
        scanf("%d", &auxiliar);
        printf("Usuário digitou %d \n",auxiliar);
        printf("Vamos somar até %d \n",2*auxiliar-1);
        soma=0;
        if (aux){
            for (i=1;i<=(2*aux-1);i++){
                soma += i};
            printf("O resultado da série é %f \n\n",soma);}
        else{
            printf("entrada terminada pelo usuário \n");}
    }
    return 0;
}
*/
